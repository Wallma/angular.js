<%= form_for(@sexual) do |f| %>
  <% if @sexual.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sexual.errors.count, "error") %> prohibited this sexual from being saved:</h2>

      <ul>
      <% @sexual.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :language_id %><br>
    <%= f.text_field :language_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
